# 🍕 PIZZYPIZZA POS System (KFC Model)

## อธิบายชื่อโครงงาน
การนำความรู้ด้านฐานข้อมูลมาประยุกต์ใช้ในการพัฒนาระบบที่สามารถใช้งานได้จริง โดยในโครงงานนี้ได้มุ่งเน้นการออกแบบและพัฒนาระบบสำหรับร้านอาหารในรูปแบบ Point of Sale (POS) ภายใต้ชื่อระบบ “PizzyPizza”

ระบบดังกล่าวถูกพัฒนาขึ้นเพื่อจัดการข้อมูลที่สำคัญ เช่น ข้อมูลเมนูอาหาร คำสั่งซื้อ และการชำระเงิน โดยใช้ฐานข้อมูลในการจัดเก็บและประมวลผลข้อมูลอย่างเป็นระบบ ช่วยเพิ่มความรวดเร็ว ความถูกต้อง และลดข้อผิดพลาดในการทำงานของร้านอาหาร

## รายชื่อสมาชิก
1.นายคอยรุลอามีรีน นาวี 6710515013
2.นางสาวเอมมิตรา แสวงทรัพย์ 6710525038
3.กัญญาภัค หอมกระจ่าง 6710615029
4.ภีมพิมล เชื่องชยะพันธ์ 6710685063


## 🚀 การติดตั้งโปรเจคสำหรับทีม (First-time Setup)

ทำตามขั้นตอนด้านล่างนี้เพื่อตั้งค่าโปรเจคในเครื่องของคุณเป็นครั้งแรก

### 1. Clone โปรเจคลงเครื่อง
เปิด Terminal (หรือ Git Bash / WSL) แล้วรันคำสั่ง:
```bash
git clone <ใส่-URL-ของ-Repository-ที่นี่>
cd PizzyPizza
```

### 2. สร้างและเปิดใช้งาน Virtual Environment
เพื่อป้องกันไม่ให้ Library ของโปรเจคนี้ไปตีกับโปรเจคอื่นในเครื่อง ให้สร้างจำลองสภาพแวดล้อมขึ้นมา (สังเกตว่าจะมีโฟลเดอร์ `.venv` ปรากฏขึ้นมา)

**สร้าง Virtual Environment:**
```bash
python -m venv .venv
```

**เปิดใช้งาน (Activate) Virtual Environment:**
* **สำหรับ Windows (Command Prompt / PowerShell):**
  ```cmd
  .\.venv\Scripts\activate
  ```
* **สำหรับ Mac / Linux / WSL:**
  ```bash
  source .venv/bin/activate
  ```
*(ถ้าทำสำเร็จ จะมีคำว่า `(.venv)` ปรากฏอยู่ข้างหน้าชื่อผู้ใช้ใน Terminal)*

### 3. ติดตั้ง Library ที่จำเป็น
เมื่อ Activate แล้ว ให้ติดตั้ง Flask และเครื่องมืออื่นๆ ที่ระบุไว้ใน `requirements.txt`
```bash
pip install -r requirements.txt
```

### 4. สร้างฐานข้อมูล (Database Initialization)
รันคำสั่งนี้ **แค่ครั้งแรกครั้งเดียว** เพื่อสร้างไฟล์ `restaurant.db` และตารางข้อมูลต่างๆ
ติดตั้ง Extension SQLite Viewer เพื่อที่จะสามารถเปิดไฟล์ restaurant.db และดูข้อมูลใน Database
```bash
python init_db.py
```

### 5. รันเซิร์ฟเวอร์
เริ่มการทำงานของเซิร์ฟเวอร์ Flask:
```bash
python app.py
```
เปิดเบราว์เซอร์แล้วเข้าไปที่ `http://127.0.0.1:5000` เพื่อดูผลลัพธ์

---

## 🛠️ ปัญหาที่พบบ่อย (Common Issues)

### ❌ 1. รันรันเซิร์ฟเวอร์แล้วขึ้น `ModuleNotFoundError: No module named 'flask'`
* **สาเหตุ:** ลืมเปิดใช้งาน Virtual Environment หรือลืมรันคำสั่งติดตั้ง requirements
* **วิธีแก้:** เช็คให้แน่ใจว่ามี `(.venv)` อยู่หน้า Terminal ถ้าไม่มีให้รันคำสั่ง Activate (ดูข้อ 2) แล้วรัน `pip install -r requirements.txt` ใหม่อีกครั้ง

# 🤝 Team Working Agreement (ข้อตกลงร่วมกันของทีม)

เพื่อให้การทำงานในโปรเจค **Pizza Fast-Food POS** เป็นไปอย่างราบรื่นและมีประสิทธิภาพ สมาชิกในทีมทุกคนต้องปฏิบัติตามข้อตกลงดังนี้:

---

## 📜 กฎเหล็ก 3 ข้อของทีม (Core Rules)

### 1. Code Documentation (คอมเม้นโค้ดเสมอ) 📝
* **ข้อปฏิบัติ:** ทุกครั้งที่มีการเขียนฟังก์ชันใหม่ หรือเขียน Logic ที่มีความซับซ้อน จะต้องเขียนคอมเม้นอธิบายเสมอ
* **จุดประสงค์:** เพื่อให้เพื่อนร่วมทีมคนอื่นสามารถอ่านและเข้าใจโค้ดได้ทันทีโดยไม่ต้องเสียเวลาสอบถามเจ้าของโค้ด และช่วยให้การทำ Code Review ง่ายขึ้น

### 2. AI Usage (Prompt First) 🤖
* **ข้อปฏิบัติ:** หากมีการใช้ AI (เช่น Gemini, ChatGPT) เพื่อช่วยในการเขียนโค้ดหรือแก้ไข Error **ต้องคัดลอกเนื้อหาในไฟล์ `project_prompt.md` ใส่ให้ AI ก่อนเริ่มแชทเสมอ**
* **จุดประสงค์:** เพื่อให้ AI เข้าใจบริบทล่าสุดของโปรเจค (เช่น Database Schema ใหม่, โครงสร้างไฟล์ และ Tech Stack ที่เราใช้) ป้องกันไม่ให้ AI แนะนำโค้ดที่ผิดพลาดหรือขัดกับโครงสร้างหลัก

### 3. Strict Branching (แยก Branch ชัดเจน) 🌿
* **ข้อปฏิบัติ:** ห้ามเขียนโค้ดหรือแก้ไขงานใน Branch `main` หรือ `dev` โดยตรงเด็ดขาด ให้สร้างและทำงานใน Branch `feature/` ตามที่ได้รับมอบหมายเท่านั้น
* **จุดประสงค์:** เพื่อป้องกัน Code Conflict และรักษาความเสถียรของโค้ดหลักในกิ่ง `dev` และ `main`

## เทคโนโลยีที่ใช้ 
Backend: ใช้ภาษา Python ร่วมกับ Framework ที่ชื่อว่า Flask
👉 ทำหน้าที่ควบคุมการทำงานของระบบ
Frontend: ใช้ HTML, CSS (ผ่าน Tailwind CSS), และ JavaScript
👉 ใช้สร้างหน้าตาเว็บให้ผู้ใช้เห็น
Template Engine: ใช้ Jinja2
👉 ใช้เชื่อมข้อมูลจาก Python ไปแสดงใน HTML